# Copiar este arquivo para ".env" (sem o .example) e preencher com valores reais.
# O arquivo ".env" NUNCA deve ser commitado no Git — só este .env.example.

POSTGRES_USER=agencia
POSTGRES_PASSWORD=TROCAR_SENHA_FORTE
POSTGRES_DB=agencia_db
N8N_HOST=seudominio.com
